#
# HiGSS <- function(B.hat,N,IDs){
#
#   est.K <- dBEMA(Stand.B = B.hat,N = N);K <- est.K$nFactors
#   data <- prepareData(B.hat = B.hat,N = N,K = K,IDs = IDs)
#
#   #fit the model for L
#   for (k in 1:K) {
#
#   }
#
#   #fit the model for G
#
#
#   #fit the model for Delta
#
#
#   return()
# }
